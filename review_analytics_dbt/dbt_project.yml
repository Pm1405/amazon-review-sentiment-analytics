name: 'review_analytics_dbt'
version: '1.0.0'
config-version: 2
profile: 'review_analytics_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  review_analytics_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
